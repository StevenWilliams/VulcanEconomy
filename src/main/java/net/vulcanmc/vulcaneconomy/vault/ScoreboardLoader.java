package net.vulcanmc.vulcaneconomy.vault;
/*
import com.github.games647.scoreboardstats.ScoreboardStats;
import org.bukkit.plugin.Plugin;

public class ScoreboardLoader {
    public static void load(Plugin plugin) {
        if(plugin.getServer().getPluginManager().getPlugin("ScoreboardStats") != null) {
            ScoreboardStats ScoreboardStats = (ScoreboardStats) plugin.getServer().getPluginManager().getPlugin("ScoreboardStats");
            ScoreboardStats.getScoreboardManager().getReplaceManager().register(new ScoreboardStatsHandler(), "VulcanEconomy");
            plugin.getLogger().info("Loaded ScoreboardStats variables: %money%, %vmoney%");
        }
    }
}
*/