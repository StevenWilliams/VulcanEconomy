package net.vulcanmc.vulcaneconomy.vault;

/*
import com.github.games647.scoreboardstats.variables.Replaceable;
import net.vulcanmc.vulcaneconomy.rest.Currency;
import net.vulcanmc.vulcaneconomy.rest.User;
import net.vulcanmc.vulcaneconomy.rest.Users;
import org.bukkit.entity.Player;

public class ScoreboardStatsHandler implements Replaceable {
    @Override
    public int getScoreValue(Player player, String variable) {
        if (("%money%".equals(variable)) || "%vmoney%".equals(variable)) {
            User user = Users.getUser(player);
            if (user == null) {
                return -2;
            }
            if (user.hasAccount(new Currency())) {
                return (int) Users.getUser(player).getAccount(new Currency()).getBalance();
            } else {
                return -1;
            }
        }
        return UNKOWN_VARIABLE;
    }
}
*/